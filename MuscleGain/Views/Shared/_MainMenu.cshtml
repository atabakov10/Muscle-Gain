@using MuscleGain.Core.Constants
<nav class="mt-3">
	<ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
		<!-- AddAsync icons to the links using the .nav-icon class
		with font-awesome or any other icon font library -->
		<li class="nav-item mb-2">
			<a href="#" class="nav-link active text-center">
				<p>
					Protein
					<i class="right fas fa-angle-left"></i>
				</p>
			</a>
			<ul class="nav nav-treeview">
				<li class="nav-item">
					<a asp-area="" asp-controller="Protein" asp-action="All" class="nav-link active">
						<i class="far fa-circle nav-icon"></i>
						<p>Protein Page</p>
					</a>
				</li>
				@if (User.IsInRole(RoleConstants.Administrator) || User.IsInRole(RoleConstants.Supervisor))
				{
					<li class="nav-item">
						<a asp-area="" asp-controller="Protein" asp-action="Add" class="nav-link">
							<i class="far fa-circle nav-icon"></i>
							<p>Add Protein</p>
						</a>
					</li>
				}
			</ul>
		</li>
	@if (User?.IsInRole("Manager") ?? false)
	{
		<li class="align-bottom">
			<a asp-area="Admin" asp-controller="User" asp-action="Index" class="nav-link active align-bottom">
				<p>
					Account Management
				</p>
			</a>
		</li>
	}
	</ul>
</nav>

